"""FastAPI application: HTTP endpoints and the chat WebSocket.

events.py defines the wire protocol shared with the frontend. Keep it in sync
with frontend/src/types/index.ts — they are two halves of one contract.
"""
