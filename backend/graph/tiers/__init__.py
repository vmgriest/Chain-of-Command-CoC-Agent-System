"""The four tiers.

All four are instances of the same loop factory in base.py, differing only in
persona, model, tool list, and prompt. If a tier file starts growing bespoke
control flow, that logic probably belongs in base.py behind a flag.
"""
