"""Company configuration: parsing, validation, and startup wiring.

Everything a company needs to change lives in company_config.json. Nothing in
backend/graph/ should ever hardcode a persona name, model id, or tool binding.
"""
